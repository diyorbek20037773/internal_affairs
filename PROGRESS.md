# PROGRESS — Huquqni muhofaza qilish ta'lim klasteri (ex O'quv klasteri, HIMOYA-360)

Status file for the autopilot loop (see `AUTOPILOT_PROMPT.md`). First unchecked item = next job.

## Done
- [x] "Mening Inspektorim" module: dashboard, AI Inspektor chat + SOP panel, Hodisa, Voice, Qonunchilik (RAG-lite), Ishlarim; uz/ru/en; Gemini key pool. (2026-09-13)
- [x] Rebrand to HIMOYA-360; 4 trainers (AI-Muloqot, Qaror, Smart Mahalla, Smart Debrifing) + HIMOYA-ID + Mentor + Bir kunlik xizmat + Instruktor kabineti. (2026-09-14)
- [x] Hujjatlashtirish trainer, Amaliy xizmat KPI card, all pptx slides covered. (2026-09-14)
- [x] TIR 3D range (VirTra V-300 style): multi-actor engine, 3 template scenarios + yard/bus-stop + plate rack, instructor station, `npm run check:tir`. (2026-09-14)
- [x] Realism: RPM avatars + mocap, Poly Haven 2k HDRI, PBR, post-fx; `scripts/fetch-assets.mjs` on prebuild. (2026-09-14/15)
- [x] Completion pass: `/api/stt`, `/api/sim/mentor`, provisional HIMOYA-ID after debrief, `/api/sim/exam-summary`, PWA, 18 scenarios. Verified live. (2026-09-15)
- [x] FPS pass: PointerLock WASD, viewmodel, centre raycast, ammo/health, game states; perf pass (3D decoupled from React, GPU-based quality). (2026-09-15)
- [x] Project docs: `CLAUDE.md`, `AUTOPILOT_PROMPT.md`, `docs/TZ_concept_ru.md`, this file. (2026-09-15)
- [x] **Credibility audit (2026-09-15)** — three audits (UX 28 items, engineering 28, content 24), most P0/P1 closed in `054978b` (tablet layout, terminology, instructor gating, real empty states, ru/en content), `ff8cc03` (legal accuracy, trilingual decision scenarios, `src/lib/aiGuard.ts` per-IP cost-weighted token bucket `AI_RATE_PER_MIN` (40/min) + 401 without a session when auth is on, honest README, `e2e/` suite in repo), `85c3299` (instructor-only ghost panel, tablet TIR briefing, ghost-session filter, ProfileGate). Follow-up: raw error `detail` only in development (`errorDetail()` in `apiErrors.ts`, used by chat/legal/tts/store), `AUTH_SECRET` required in production (login disabled + store reported local otherwise), TIR toolbar and devices panel touch targets ≥40 px on tablet.
  - Open P1s closed 2026-09-16: `69e09d1` SW `VERSION` from the build id (`scripts/build-sw.mjs` on prebuild, `public/sw.js` generated + git-ignored, reload on `controllerchange`) + dialog portrait turn counter; `bc9c642` instructor cabinet (search/group/kind/period filters, `ExamSignoff` + `h360_exam_signoffs` + `/api/store/signoffs`, printable `TraineeReport` via `@media print`); `8087b14` instructor station across devices (`src/lib/tir/relayStore.ts` in-memory ring, `/api/tir/station`, `stationLink.ts` = BroadcastChannel + 1 s poll, `LiveRanges` picker); 3 new trilingual scenarios — `mahalla-ch07` (market/school district: drunk driver, phone fraud, drug signs), `document-mast-haydovchi` (H-03, refusal of medical exam, child in car), `document-reyd-hisobot` (H-04, playground patrol with minors).
  - Verify: Playwright (scratchpad `pw/{cabinet,station}.js`) 12/12 and 7/7 (two browser contexts → server relay, pause + ALL STOP from the station), `check:tir` 21/21, build clean.
  - Still open (needs infra/content, not code): Postgres/Redis-backed relay for multi-instance Railway; server-side PDF; filmed video packs; WebXR on real headsets.
- [x] **Huquqni muhofaza qilish ta'lim klasteri — multi-agency platform + fast voice** (2026-09-25)
  - Brand: «O'quv klasteri» → «Huquqni muhofaza qilish ta'lim klasteri» (ru «Образовательный кластер правоохранительной деятельности», en «Law Enforcement Education Cluster») in UI, manifest, metadata, PDF, AI prompts. Nav: new «Ta'lim klasteri» group (Kasb standartlari, O'quv klasterlari, Kasb simulyatorlari, AI-Tutor, Kompetensiya pasporti), IIV trainers, «Mening Inspektorim» labelled as the prevention-inspector module. Home: quick links, professions explorer, dynamic standards chain.
  - Voice (AI-Muloqot): `src/lib/voice/recorder.ts` (mic → 16 kHz WAV directly, VAD auto-stop ~1.1 s silence, level meter); `useSpeech` `autoStop`/`direct`/`enqueueSpeech` + WebAudio TTS queue with prefetch; `useSentenceSpeaker` speaks the first sentence while the reply still streams. `/api/sim/dialog` accepts `audio` — the model transcribes (`heard`, streamed as its own SSE event) and answers in ONE call (no `/api/stt` round-trip); `adaptiveBudgetInstruction` keeps reply length rules for voice turns; `propertyOrdering` on the citizen schemas. «Jonli suhbat» hands-free mode. TTS route LRU cache. Fixed: «Mening Inspektorim» voice never sent the server-STT transcript; TIR voice commands auto-stop.
  - Professions (`data/kasblar/`): 5 agencies (IIV, Milliy gvardiya, Bojxona, Prokuratura, FVV), 11 profession standards (functions, knowledge, skills, 5 competencies each, 4 levels, education, practice, legal basis, assessment, career), 7 learning clusters with subjects. Pages `/kasblar`, `/kasblar/[id]`, `/klasterlar`, `/pasport` (`src/lib/kasb/passport.ts`: strengths/gaps/recommended subjects+sims/next career step; profilaktika blends Klaster-ID).
  - Profession simulators (`data/kasblar/sims/`, `/kasb-simulyator`): 11 sims, one per profession, each in its own environment UI (case file, duty desk, crime scene, cyber sandbox, customs post, analytics heatmap, prosecutor review, counseling, guard post, emergency); task kinds choice/multi/order/numeric/match/grid/text; `src/lib/kasb/simEngine.ts`; text tasks graded by `/api/sim/kasb-grade`; results via `kasbRepo` (`src/lib/storage/kasb.ts`).
  - AI-Tutor (`data/tutor/`, `/tutor`, `/api/tutor` SSE): 27 work-process topics across the 5 agencies, Socratic Q&A from an authored lesson card, 2–3 quiz items per topic, voice + hands-free.
  - Verify: `npm run check:kasb` 53/53 + 39/39, `check:dialog` 22/22, `check:decision` 30/30, build clean; `e2e/kasb.js` on `next start` — 19 routes × tablet/desktop, no overflow/errors/raw keys, sim T-01 starts.
  - Open: facts to confirm with subject experts (tutor: appeal deadlines, CPR 30:2, 103/112, customs valuation methods; prokuror sim: 72 h detention limit — no article cited); instructor cabinet does not yet see profession-sim/tutor results (local only); voice path needs a live check on Railway (no local keys).

## Backlog (in order)
- [x] **Server persistence (Postgres)** — `/api/store/{profile,sessions,himoya-id}` backed by `DATABASE_URL` (auto-migrate), `TrainingRepo` hybrid: local write-through + server when reachable; instructor cabinet sees all trainees across tablets; no `DATABASE_URL` → local-only, unchanged behaviour. (2026-09-15)
  - How: `src/lib/db/pg.ts` (pool + idempotent schema `h360_profiles/sessions/himoya_ids`, jsonb), `src/lib/storage/serverStore.ts` (last-writer-wins by `updatedAt`), `src/lib/storage/trainingRemote.ts` (fetch client, cached `/api/store` mode probe, 60 s re-check), `hybridTrainingRepo` = default `trainingRepo`. `GET /api/store` → `{mode}`; `/api/health.store`.
  - Verify: local `docker run postgres` + `DATABASE_URL=… next dev` → curl PUT/GET/DELETE; Playwright two-context test (scratchpad `pw/store.js`) 8/8: offline-seeded session uploads on list, instructor on another device sees it, blocked API → local fallback, no page errors.
  - Railway: add Postgres plugin, set `DATABASE_URL=${{Postgres.DATABASE_URL}}` on the app service. Until then live site stays local-only.
- [x] **Auth (MVP)** — badge ID + PIN login backed by the same DB, instructor role server-side, `traineeId` from session; keep local-profile fallback when no DB. (2026-09-15)
  - How: `src/lib/auth/server.ts` (table `h360_auth`, scrypt PIN hash, HMAC token in httpOnly cookie `h360_sid`, 30 d, 5 wrong PINs → 1 min lock), `/api/auth/{register,login,logout,me}`, `INSTRUCTOR_BADGES` env decides role. All `/api/store/*` routes require a session: trainee sees only own rows, instructor everything; role/badge can't be changed by the client. Client: `useTraineeProfile` → `/api/auth/me`, `AuthForm` on `/profil` when `authRequired`, server profile mirrored to localStorage; `storeMode()` is `postgres` only when logged in, else local. No `DATABASE_URL` → `/api/auth/*` 501, old device-profile flow untouched.
  - Verify: local Postgres + `next start` with `DATABASE_URL`, `AUTH_SECRET`, `INSTRUCTOR_BADGES=SH-0001`: curl register 201 / dup 409 / wrong PIN 401 / lockout 423 / store w/o cookie 401; Playwright two-tablet UI test (scratchpad `pw/auth.js`) 12/12; no-DB server → `me {enabled:false}`, store `local`.
  - Railway: set `AUTH_SECRET` (long random) and `INSTRUCTOR_BADGES` next to `DATABASE_URL`.
- [x] **E-O'quv embed mode** — `?embed=1` (no sidebar/header), `postMessage` bridge: tablet app passes profile in, receives session results out; docs for the tablet vendor. (2026-09-15)
  - How: `src/lib/embed/bridge.ts` (protocol v1; iframe `window.parent.postMessage` + Android WebView `H360Host.postMessage` / `window.h360.receive`), inline boot script in the locale layout sets `<html data-embed="1">` before hydration and remembers the flag in `sessionStorage`; `.h360-chrome` (sidebar, topbar) hidden by CSS. `EmbedBridge` component: `h360:ready/profile/session/himoya-id/navigate/error` out, `h360:set-profile/navigate/get-sessions/logout` in. Sessions emitted from `localTrainingRepo.saveSession` when `completed`. Inbound iframe messages only from `window.parent` with origin in `NEXT_PUBLIC_EMBED_ORIGINS` (or same-origin). Demo host: `public/embed-demo.html`. Docs: `docs/embed-bridge.md` (ru).
  - Verify: Playwright (scratchpad `pw/embed.js`) 13/13 on `next start`: ready/profile echo, chrome hidden before+after navigation, set-profile → localStorage, get-sessions → session events, non-parent message ignored, logout, plain `/uz` unaffected.
  - Railway: set `NEXT_PUBLIC_EMBED_ORIGINS` to the E-O'quv web origin if it frames us (WebView needs nothing).
- [x] **Laser gun / HID input adapter** — WebHID + Gamepad + keyboard mapping → `onShoot`; settings UI to bind a device; docs. (2026-09-15)
  - How: `src/lib/tir/input.ts` — `inputHub` singleton (Gamepad API polled per frame, edge-triggered; WebHID device picked once, reattached via `getDevices()`, rising bits of input reports = presses), bindings in localStorage `h360:tir:input:v1` (Xbox defaults), dispatches `h360:input {action}`; `ShotRaycaster` listens for `fire` (FPS: camera centre; fixed mode: mounted with `pointer={false}`, camera auto-aims); other actions mapped in `TirClient` like the keys. UI: USB button over the scene → `InputDevicesPanel` (status, pick HID, "bind → press button" capture, reset). Docs `docs/tir-input-devices.md` (ru). Mouse-emulating light guns need nothing.
  - Verify: Playwright with a fake `navigator.getGamepads` (scratchpad `pw/hid.js`) 12/12: detection, capture-binding persisted, draw/fire/reload via pad in FPS and fixed modes, unmapped button ignored, raw `h360:input` event fires; `check:tir` 21/21.
- [x] **Shock belt output adapter** — `h360:shock` → Web Serial / Web Bluetooth bridge with a test button; docs. (2026-09-15)
  - How: `src/lib/tir/shock.ts` — `shockHub` listens to `h360:shock`, writes a templated payload (`SHOCK {ms} {level}
` text or `hex:…` bytes with `{ms16}`) over Web Serial (port remembered via `getPorts()`) or Web Bluetooth GATT (service/characteristic configurable, HM-10 defaults); config in localStorage `h360:tir:shock:v1`; `h360:shock-sent` after each write. UI: shock block inside the USB devices panel (transport, connect/disconnect, enabled flag, duration/intensity/baud/UUIDs, template, test pulse, sent counter). Docs `docs/tir-shock-belt.md` (ru, Arduino example, safety note).
  - Verify: Playwright with a fake `navigator.serial` (scratchpad `pw/shock.js`) 10/10: connect, test pulse bytes, `h360:shock` gated by the enabled flag, config persisted, hex template encoding, counter.
- [x] **Video packs** — software done, needs filmed content. `TirVideoLayer` plays per-actor-state clips over the 3D scene when a scenario carries a `video` pack; spec + shooting list in `docs/tir-video-pack.md`. Nothing more to code until clips exist. (2026-09-16)
- [x] **WebXR mode for TIR** — VR entry button when `navigator.xr` present, controller trigger = shoot. (2026-09-15)
  - How: `src/components/training/tir/xr/XrRig.tsx` (plain `renderer.xr`, no extra dep): `h360:xr {cmd}` from the VR button → `requestSession("immersive-vr", local-floor)`; XR camera parented to a rig that follows the FPS body (fps mode) or the officer spot (fixed); controller `selectstart` = hit-scan along the controller ray (same actor/zone resolver as the mouse, exported from `ShotRaycaster`), `squeezestart` = reload (via `h360:input`), left thumbstick = walk with `clampOfficer`. In VR: post-fx and flat viewmodel off. `h360:xr-state {presenting,error?}` → toolbar state / toast. Docs `docs/tir-webxr.md` (ru).
  - Verify: Playwright with a fake `navigator.xr` (scratchpad `pw/xr.js`) 7/7: button only when supported, click → `requestSession` with local-floor, failure → toast + scene alive, engine ticks with rig mounted; `check:tir` 21/21. Real headset run (Quest browser) still to be done on hardware.
- [x] **Postgres-backed TIR relay (multi-instance Railway)** — `src/lib/tir/relayStore.ts` now has two backends behind one async API: `h360_tir_rooms` / `h360_tir_relay` (jsonb, auto-migrated) when `DATABASE_URL` is set, the in-memory ring otherwise; a failing DB call falls back to memory for that call (relay degrades to single-instance, never down). Station polls touch `last_station_at` (picker shows "station attached"), range polls are read-only; rooms idle 10 min are swept every 30 s; per-room ring trimmed to 200. `/api/health` reports `tirRelay: postgres|memory`. (2026-09-16)
  - Verify: local `docker run postgres:16-alpine`, two `next start` instances (3101/3102) on one `DATABASE_URL`: curl range→A, station list/late-join/cmd→B, range reads cmd on A (9 steps); Playwright (scratchpad `pw/relay-pg.js`, fake range via fetch on A + real instructor station UI on B) 9/9; same script against a no-DB instance (memory) 9/9; build clean.
  - Railway: nothing extra — same `DATABASE_URL` as the store. Scale to N replicas freely.
- [x] **Server-side PDF for the trainee report** — `POST /api/report/pdf` (instructor-only when auth is on) renders a real A4 PDF with `pdfkit`; the cabinet's report card has a "PDF yuklab olish" button next to print. (2026-09-16)
  - How: the client sends an already-localised `ReportDoc` (labels, HIMOYA-ID rows, exams, up to 200 sessions — `src/lib/pdf/traineeReport.ts` zod schema); the server lays it out with embedded DejaVu Sans (`src/lib/pdf/fonts/`, Bitstream Vera licence) so Uzbek Latin `oʻ/gʻ` and Cyrillic render; header, HIMOYA-ID bars, exam verdicts, paginated sessions table with repeated header, signature lines, page numbers. `pdfkit` is in `serverComponentsExternalPackages`. Filename `himoya360-<badge>.pdf`.
  - Verify: curl sample doc → `200 application/pdf`, 2 pages (rendered via pdf.js, checked visually); `{"x":1}` → 400; Playwright (scratchpad `pw/report-pdf.js`) cabinet → report → download: PDF magic bytes, ru locale, no page errors (7/8 — the 8th expected a badge id in the filename, which device mode cannot know: it uses the trainee id, same as the report header).
- [x] **WebXR on a real headset** — software done, needs hardware (Quest/Pico run); when done, log findings in `docs/tir-webxr.md`. (2026-09-16)
- [x] **Dependency audit + e2e hygiene** — `npm audit fix` (non-breaking): drei/uuid, brace-expansion, esbuild/tsx, js-yaml, nanoid closed; build + `check:tir` 21/21 unchanged. Scratchpad checks moved into the repo: `e2e/relay.js`, `e2e/station.js`, `e2e/report-pdf.js` (+ `npm run e2e:*`, README). (2026-09-16)
  - Remaining advisories need majors: `next` 14.2.35 (GHSA-h25m-26qc-wcjf — RSC Server-Function request DoS; we ship **no** Server Actions, so no such endpoint is registered; GHSA-9g9p — Image Optimizer `remotePatterns`, not configured), `next-intl` 3 → 4 (open redirect / precompile prototype pollution — we use neither `localePrefix` redirects from user input nor `experimental.messages.precompile`), `eslint-config-next` (glob CLI, dev-only), `postcss` via next (build-time). Tracked as the next item.
- [x] **Upgrade Next 14 → 15 (+ React 19, next-intl 4, r3f 9 / drei 10)** — `next` 15.5.25, `react` ~19.2 (r3f 9.7 caps React <19.3), `@react-three/fiber` 9.7, `drei` 10.7, `@react-three/postprocessing` 3.1.1, `next-intl` 4.14, `eslint-config-next` 15; `three` stays 0.160.1 with `postprocessing` pinned 6.36.4. `npm audit`: 6 → 2 (postcss inside next, esbuild via tsx — build/dev-only). (2026-09-16)
  - How: lockfile regenerated; `@next/codemod next-async-request-api` → 25 pages/layout now `await props.params / searchParams`; `serverExternalPackages: ["pdfkit"]`. Two bugs found by the e2e run and fixed (both pre-existing on 14): TIR hit overlay used `t("shock")` after the shock-belt panel turned `sim.tir.shock` into an object → new `sim.tir.shockHit`; `remoteAuth.me()` 1.5 s shared-answer cache was served stale right after login/register/logout → cache dropped on those calls.
  - Verify: build clean, `check:tir` 21/21, `check:tir:fps` 19/19; e2e against `next start`: `tir` 4/4, `fps` 18/18 (no console errors), `station` all pass, `crawl` 46/46 pages desktop+tablet 0 errors / no overflow, `relay` 9/9, `report-pdf` pass, `embed` 13/13 (device mode), `cabinet` 12/12, `hid` 12/12, `shock` 10/10, `xr` 7/7; with local Postgres + `AUTH_SECRET`: curl register/dup/me/wrong-PIN/store 201/409/200/401/401→200, `auth` 12/12. AI routes keep the contract (no keys → 500 `no_keys_configured`). `e2e:qa` needs real Gemini keys — run against live after deploy. New `e2e/auth.js`, `e2e/embed.js`.
  - `next lint` was never configured in this repo (Next 15 prompts interactively) — left as is.
  - Live (Railway, SW `h360-61bf89a5b5a8`): `e2e:crawl` 46/46 pages 0 errors, `e2e:qa` 21/21 with real Gemini (dialog, debrief, decision, mahalla, document AI check, HIMOYA-ID, exam day, ru).- [x] **AI-Muloqot: reply length + streaming** — the virtual citizen answered in ~2 short sentences whatever the officer said, and each turn showed nothing for 2–5 s. (2026-09-18)
  - Evidence (live probe, 7 scenarios × 4 officer-utterance types, 28 calls): an open "tell me everything" invitation came back with 17–53 words, closed questions 4–13; per-turn latency 1.7–4.8 s (mean ~2.6 s) with no output until the whole JSON envelope was ready. Session end was NOT the problem — no scenario ended before 4 turns.
  - How: `src/lib/training/replyBudget.ts` decides the length server-side per turn from the officer's utterance (open invitation / identity ask / ordinary question / long speech) and the hidden state (low trust → shorter, high cooperation → longer, tension ≥ 80 → curt), with a deterministic ±10 % wobble so consecutive turns differ; `budgetInstruction()` injects a hard `BU NAVBATDAGI JAVOB HAJMI` block into the citizen prompt (qisqa 5–22, orta 35–70, batafsil 95–165, portlash 70–130 words). `/api/sim/dialog` gained `stream: true` → SSE (`delta` frames carry the reply as it is generated, `final` carries assessment + state + end); `streamJsonRaw()` in the Gemini client + `partialString()` (`src/lib/gemini/partialJson.ts`) read the `reply` property out of half-written JSON. The first chunk is pulled before the 200 is committed, so the `no_keys_configured` / `ai_unavailable` / `bad_ai_output` contract is unchanged; a malformed stream falls back to one non-streaming attempt. Client: `useDialogSim` reads the SSE and exposes `streamingReply`, the chat bubble types it out with a caret.
  - Verify: `npm run check:dialog` 16/16 (budget classification + partial-JSON decoding), build clean. Live re-probe (same 7×4 matrix): open invitation now 48–86 words (was 17–53), identity asks 2–8 (was 4–13), mid-dialogue turns 15–44 — length now tracks the situation. SSE: first token 1.48 s (was a 2.6–4.8 s blank wait), streamed text identical to the final reply; `e2e:dialog` passes on live (3.0 s, trend chip, no console errors).
  - Noted, not fixed: the Gemini key pool is running hot on free-tier quota (health: 9/35 keys cooling, failovers > requests), so a burst of turns can still answer 503 `ai_unavailable`. That is capacity, not this code path.
- [x] **Gemini key pool: round-robin instead of random** — every request now starts at the key after the previous request's, so 35 free-tier keys carry an even share instead of colliding by chance. (2026-09-18)
  - How: `pickKey()` in `src/lib/gemini/keyPool.ts` walks the pool from a module-level cursor, skipping cooling keys (the cursor advances past the key it hands out); the all-cooling fallback (soonest to recover) and the per-request failover are unchanged. `__resetPool()` added as a test seam.
  - Also: a 503/504 from Gemini says nothing about the key, but parked it for 15–30 s, so a burst of 12 parallel turns left **30 of 35 keys cooling** and answered 5 × 503 (measured on live before the fix; `lastError` was `504 DEADLINE_EXCEEDED`, not quota). Cool-downs are now graded — quota 60 s and invalid 30 min (the key's fault) vs overloaded 8 s and timeout 5 s (Gemini's) — and `coolKey()` caps cooling at half the pool, releasing the soonest-to-recover keys early.
  - Correction after the first live re-measure: the cap must not release a key parked for **quota** — it answers 429 again and wastes an attempt (sequential test: 6/15 turns, 503 in ~1 s). Only `overloaded`/`timeout` parks are releasable; `/api/health` now reports `coolingByKind`.
  - Live measurement of the pool itself: Gemini's free-tier quota is per MINUTE (the pool went 12 → 32 healthy in 75 s with no other change), and the live `GEMINI_API_KEYS` holds **3 permanently invalid keys** plus keys that appear to share a project, so a sequential run of 15 turns still parked 20+ keys on 429. `GEMINI_MAX_ATTEMPTS` default raised 6 → 12: a 429 returns in ~150 ms, so walking further through a 35-key pool is cheap and avoids answering 503 while keys are still usable. Remaining capacity work is account-side (replace the 3 dead keys, spread keys across separate Google Cloud projects, or move to a paid tier) — `/api/health.gemini.coolingByKind` shows which wall is being hit.
  - Then the wall showed itself: after the 60 s cool-down the pool reported 32 healthy keys, but ONE request burned 12 of them on instant 429s and still failed — a **daily** (RPD) quota, not a per-minute one, and the keys behave as if they share a project. `quotaScope()` now reads Google's quota metric: a per-day exhaustion parks the key for 3 h (`quota_day`) instead of 60 s, so later requests stop wasting attempts on dead keys; `lastError` keeps 300 chars so the metric name is visible in `/api/health`.
  - Final shape: Gemini states the wait itself (`limit: 20, model: gemini-2.5-flash … Please retry in 22.037s`), so `retryAfterMs()` parks the key for exactly that (+10 % slack, capped at 3 h) and a wait over 5 min is treated as a daily exhaustion. `/api/health.gemini.lastError` now shows the metric line, which is how the limit was identified.
  - **The actual capacity wall (live, 2026-09-18):** all 32 usable keys answer the SAME error — `Quota exceeded for metric generate_content_free_tier_requests, limit: 20, model: gemini-2.5-flash`, with the retry delay growing as load rises. A per-key limit would not fire for every key at once, so the keys share one Google Cloud project's free-tier quota: 35 keys ≠ 35× capacity. 3 more keys are permanently invalid. Fixing this is account-side — separate projects per key, or a paid tier — not code.
  - Verify: `npm run check:keys` 21/21 (order, wrap-around, even 100/100/100/100 split over 400 picks, quota-cooled key skipped, failover across 4 distinct keys, fatal error not retried, cooling cap holds under an all-keys failure wave), build clean.
- [x] **Gemini: quota parked per model + flash-lite fallback when flash runs out** — "AI vaqtincha band" on the AI simulator: live health showed 11 requests → 97 failovers → 7 × 503, all on `generate_content_free_tier_requests, limit: 20, model: gemini-2.5-flash … retry in 28s`. (2026-09-18)
  - Cause in code: the free-tier daily wall suggests "retry in ~30 s", so dead keys were released every half minute and each request burned its attempts on them; and a TTS 429 parked the key for chat too, although Google counts quota per (project, model).
  - How: `keyPool.ts` parks a (key × model) slot, not a key (invalid keys still apply to every model). A slot that hits quota again within 5 min is parked 30 min; an explicit per-day quotaId beats a short retry hint. When no key has primary-model quota left, `withKeyFailover()` switches to `GEMINI_FALLBACK_MODEL` (flash-lite, own quota) instead of answering 503; callers take `ctx.model` (`modelFor` removed). `/api/tts` runs on its own model slot without fallback. `/api/health.gemini.fallback` shows flash-lite capacity.
  - Verify: `npm run check:keys` 29/29, `check:dialog` 16/16, build clean. Capacity itself is still account-side (20 req/day per project per model on the free tier).
- [x] **Rebrand + TIR last + mixed-task Qaror simulyatori** (2026-09-18)
  - Brand: every user-visible "HIMOYA-360" → «O'quv klasteri» (ru «Учебный кластер», en «Training Cluster»), "HIMOYA-ID" → «Klaster-ID»: messages, manifest, layout metadata, PDF report/filename, AI system prompts. Internal `h360_*` keys/tables/cookies and the `HimoyaId` type keep their names so stored data survives. TIR is the last card in the trainers grid.
  - Qaror simulyatori: the 4 multiple-choice scenarios are replaced by ONE scenario, `decision-uchinchi-qavat` (Q-01 «3-qavatdagi chaqiruv», domestic violence call, trilingual), with five task kinds: `scan` (free taps on an SVG stairwell scene, matched to authored hotspots only on submit — distractors and empty taps cost), `choice` (timed, legality/proportionality as before), `voice` (server STT, 60 s cap, transcript editable, typing fallback, TTS of the situation), `order` (tap-to-sequence priorities), `text` (report narrative). Non-choice tasks score 0–3 and the score picks the branch (a weak opening to the aggressor → extra escalation node `n3x`).
  - How: `DecisionNode.task` discriminated union (`data/scenarios/types.ts`), `submitTask`/`resolveBranch`/`scoreScan`/`resolveTaps`/`scoreOrder`/`scoreRubric` in `decisionEngine.ts`, step schema gains `task/score/response/picks/misses/rubric/feedback/ungraded`. New `POST /api/sim/task-grade` (`prompts/task-grader.uz.ts`, `generateJson`, rubric read server-side, violation or < minWords caps at 1, error contract unchanged). AI down → Retry or «Baholamasdan davom etish»: branches as score 2 and the step is left out of competency scoring, flagged for the instructor. `decisionDeterministic` feeds each task's competencies; debrief evidence + transcript show every task kind. UI: `components/training/decision/tasks/{ScanTask,OrderTask,SpeakWriteTask}.tsx`, task track in the right rail.
  - Verify: `npm run check:decision` 30/30 (graph, laws, scan/order/rubric scoring, branching, optimal walk → success, ungraded neutrality), `check:tir` 21/21, `check:dialog` 16/16, build clean; Playwright on `next start` at 1024×768 (scratchpad `qaror.js`) 18/18 — TIR last, no "HIMOYA" in the page, full walk scan → choice → voice (no local keys → ungraded path) → order 3/3 → voice → timed choice → text → success → debrief lists task kinds, no overflow, no page errors. `e2e/qa.js` decision section rewritten for the new flow (needs live Gemini).
